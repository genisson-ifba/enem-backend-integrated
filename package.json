{
  "name": "enem-backend",
  "version": "1.0.0",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "build": "echo 'No build step needed for Node.js'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "docker:build": "docker build -t enem-backend .",
    "docker:run": "docker run -p 8000:8000 --env-file .env enem-backend"
  },
  "keywords": [
    "enem",
    "education",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "description": "API backend para simulador ENEM",
  "dependencies": {
    "@fastify/cors": "^11.1.0",
    "@fastify/helmet": "^13.0.1",
    "@fastify/rate-limit": "^10.3.0",
    "@fastify/static": "^8.2.0",
    "cors": "^2.8.5",
    "fastify": "^5.5.0",
    "marked": "^15.0.12"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}
